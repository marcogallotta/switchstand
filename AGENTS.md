# Switchstand agent routing

## Authority
Start from the exact active work/WorkId and governing references. An Asana task ID is not a WorkId and preparing a task is not dispatch/binding.

Role, Priority, visibility, tool/login access and project membership never grant effects. Current work/WorkId, explicit authorization/grant and writable surface do. References are read-only unless separately granted.

`~/.claude/CLAUDE.md` is not authoritative for Switchstand. Repository `CLAUDE.md` only points back here. Before material effects/replacement writes, reread current state/preconditions; preserve STALE/DENIED/UNKNOWN, read back success and never blind-retry ambiguous effects.

## Current Codex host/bootstrap
Use repository `docs/development.md` at the exact launch/green repository SHA for current host/bootstrap mechanics. Supported managed launch binds one active task plus explicit read-only references; a fresh run calls `work_get` without a WorkId for its active assignment and may read only the launch-bound reference WorkIds it is given. Only the active WorkId is writable.

A procedure’s Asana task ID appearing below does not make it a WorkId or readable reference. If a required Asana procedure is not launch-bound as a read-only reference, affected governed work is BLOCKED/UNAVAILABLE unless temporary bridge 1218278195229493 was explicitly activated for those exact task IDs. Never broaden Asana discovery or infer future V_N capability.

## Process / re-entry
Load the applicable current procedure/Contract/Plan on fresh/replacement run, after compaction/context summary, when entering research/design/review/implementation/qualification-release/incident work, and before a consequential external effect if its procedure is not grounded. At a material role/phase/gate change reread when current procedure content is no longer grounded; do not reload every turn.

Routes:
- Human Review/steering: 1218212398415468
- Evidence/claim verification: 1218223005031323
- Implementation materiality/realization envelope: 1218218678917862
- Roles/review/coordinator/work lifecycle: 1218387133916430
- Temporary direct-Asana bridge: 1218278195229493
- Unscoped/current routing: START HERE 1218327002478382
- Incident entry: exact assigned incident owner/live state; if unscoped resolve current incident pointers through START HERE.

Docs may require runtime truth, receipts, isolation or feedback; they do not make those capabilities exist. Missing/unqualified capability is BLOCKED/UNKNOWN.

## Ownership / continuation
Use one durable owner per concern and one writable owner surface. Bounded contributors/children write only assigned result/work surfaces; temporary children default under the parent and off-project. One off-project checkpoint per active owner may carry takeover state, never authority/backlog. Before final/handoff, reconcile live children, absorb durable results and retire temporary work.

Continue authorized executable work until completion or a real stop. Direct Marco STOP/correction/exact-next-action pre-empts stale plans.

## Review / implementation
Role changes behavior, never authority. For a fresh independent review, exclude prior overall verdicts, scores and author narrative unless an exact prior finding is itself the focused target.

Within an approved realization envelope, reversible private technical choices/tests/diagnostics/supported mechanisms are worker-owned unless they change product semantics, authority, external effects, isolation, recovery, approved scope, persistence/interfaces or another hard-to-reverse outcome. Escalate changed outcome/boundary, not diff size.

Configured/intended state is not proof. Prefer deterministic diagnosis/readback for material runtime/config behavior and required exact-environment canaries. Cheap deterministic/unit/controller tests run freely; reserve expensive real-agent/exact-host qualification for meaningful candidate boundaries required by procedure.

Before publication, run affected tests and the required quality gate and verify it executed. Treat structural/CI/performance deterioration as evidence for mechanical audit/ratchets, not prose compliance.

Use only the currently authorized visibility/friction sink. `~/.config/switchstand/friction.md` is temporary while explicitly required. Do not overwrite another run’s active/reference binding or global candidate/config state; current managed launch uses launch-scoped authority. Do not claim future V_N isolation before exact qualification.

Treat `marcogallotta/switchstandold` as read-only evidence, never an implementation base.
