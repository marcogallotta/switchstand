#!/bin/sh
set -eu

repo=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)
echo "warning: scripts/switchstand-start is deprecated; use scripts/switchstand --isolated" >&2
exec "$repo/scripts/switchstand-isolated-launch" "$@"
