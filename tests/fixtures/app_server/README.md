# App Server protocol-shape fixtures

These payloads exercise the exact field names in the current generated App Server protocol
and official documentation. They are **not recorded live observations**. The Stage A live
checkpoint remains blocked until Switchstand can connect to a real App Server containing a
root thread and spawned descendant.
